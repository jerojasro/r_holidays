== Holiday "calculator"

This repo contains a library for defining and calculating the days of
holidays for a given country, for a given year (at lib/holiday.rb), and a small
web application to allow checking the holidays for a given year.
